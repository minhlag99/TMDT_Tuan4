@model IEnumerable<string>
@{
    ViewBag.Title = "GPU";
}
<div id="10">
    @foreach (var item in Model)
    {
        @Html.CheckBox(item);
        @Html.Label(item);
        <b>&ensp;</b>
    }
</div>


