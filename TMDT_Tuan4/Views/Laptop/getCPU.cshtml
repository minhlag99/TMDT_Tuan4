@model IEnumerable<string>
@{
    ViewBag.Title = "getCPU";
}

<div id="6">
    @foreach (var item in Model)
    {
        @Html.CheckBox(item);
        @Html.Label(item);
        <b>&nbsp;</b>
    }
</div>


